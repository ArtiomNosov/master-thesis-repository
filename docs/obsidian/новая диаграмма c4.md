@startuml
!include <C4/C4_Container>

title Архитектура C4 Container: разрабатываемая система с пометками статуса компонентов

LAYOUT_LEFT_RIGHT()

AddElementTag("architectural_assumption", $bgColor="#FFF8E1", $fontColor="#1F2933", $borderColor="#F57F17", $legendText="Архитектурное допущение")
AddElementTag("architectural_approach", $bgColor="#E3F2FD", $fontColor="#1F2933", $borderColor="#1565C0", $legendText="Архитектурный подход")
AddElementTag("conceptual_component", $bgColor="#F3E5F5", $fontColor="#1F2933", $borderColor="#6A1B9A", $legendText="Концептуальный компонент")
AddElementTag("studied", $bgColor="#F1F8E9", $fontColor="#1F2933", $borderColor="#558B2F", $legendText="Исследовано в работе")
AddElementTag("implemented", $bgColor="#E8F5E9", $fontColor="#1F2933", $borderColor="#2E7D32", $legendText="Реализовано в работе")
AddElementTag("experimental", $bgColor="#E0F2F1", $fontColor="#1F2933", $borderColor="#00695C", $legendText="Экспериментально исследовано")

AddRelTag("implemented_rel", $textColor="#2E7D32", $lineColor="#2E7D32")
AddRelTag("experimental_rel", $textColor="#00695C", $lineColor="#00695C")
AddRelTag("assumption_rel", $textColor="#F57F17", $lineColor="#F57F17")

Person(hr, "HR / рекрутер", "Работает с откликами и результатами анализа в ATS", $tags="conceptual_component")

System_Boundary(reqcore_system, "ATS Reqcore + модуль семантического ранжирования\nАрхитектурный подход") {

  Container(reqcore_web, "Reqcore Web Application", "Python / Web UI", "Пользовательский интерфейс ATS для работы с вакансиями, откликами и результатами анализа", $tags="implemented")

  Container(reqcore_backend, "Reqcore Backend", "Python / REST API", "Основная серверная логика ATS. Выполняет анализ заявки и выбирает путь LLM-scoring или biencoder-scoring", $tags="implemented")

  ContainerDb(reqcore_db, "Reqcore Main Database", "PostgreSQL", "Хранит заявки, вакансии, parsedContent документов, application.score, analysis_run, criterion_score", $tags="implemented")

  Container(biencoder_api, "ATS Semantic Ranking Engine API", "Python / FastAPI", "Микросервис семантического ранжирования. Реализует /index/candidate, /search/rank, /score", $tags="implemented")

  Container(model_runtime, "Bi-encoder model runtime", "SentenceTransformer / cointegrated/rubert-tiny2", "Кодирует тексты вакансий и резюме, вычисляет cosine similarity", $tags="experimental")

  Container(candidate_index, "Candidate Index", "In-memory list", "Временный in-memory пул кандидатов CANDIDATE_INDEX для ranking API", $tags="implemented")

  Container(offline_pipeline, "Offline data & training pipeline", "Python scripts", "Подготовка данных, деидентификация, сбор пар, split train/val/test, fine-tuning модели", $tags="implemented")

  ContainerDb(training_data, "Training data artifacts", "TSV / SQLite / local files", "resumes, vacancies, HR events, features, split-файлы и артефакты обучения", $tags="studied")

  ContainerDb(vector_db_future, "ANN / Vector DB", "Milvus / DiskANN / SPANN", "Проектное направление масштабирования поиска; в текущей работе не реализовано", $tags="architectural_assumption")

  Container(fair_ltr_future, "LTR / fair ranking layer", "Future work", "Концептуальный слой переранжирования и fairness-аудита; не проверен экспериментально", $tags="conceptual_component")
}

System_Ext(llm_provider, "External LLM provider", "OpenAI / Anthropic / Google", "Используется стандартным LLM-scoring путем Reqcore", $tags="implemented")

Rel(hr, reqcore_web, "Работает с ATS через браузер")
Rel(reqcore_web, reqcore_backend, "Вызывает REST API", $tags="implemented_rel")
Rel(reqcore_backend, reqcore_db, "Читает и записывает данные заявок, вакансий и результатов анализа", $tags="implemented_rel")

Rel(reqcore_backend, biencoder_api, "Вызывает /score по BIENCODER_URL для ветки provider = biencoder", "HTTP/JSON", $tags="implemented_rel")
Rel(reqcore_backend, llm_provider, "Вызывает LLM API для критериального scoring", "HTTP/JSON", $tags="implemented_rel")
Rel(biencoder_api, model_runtime, "Использует модель для кодирования текстов и расчёта match_score", $tags="experimental_rel")
Rel(biencoder_api, candidate_index, "Хранит и читает пул кандидатов для /index/candidate и /search/rank", $tags="implemented_rel")
Rel(reqcore_backend, reqcore_db, "Сохраняет application.score и analysis_run после biencoder-scoring", $tags="implemented_rel")

Rel(offline_pipeline, training_data, "Читает исходные данные и формирует обучающие выборки", $tags="implemented_rel")
Rel(offline_pipeline, model_runtime, "Готовит и сохраняет fine-tuned модель", $tags="experimental_rel")
Rel(training_data, offline_pipeline, "TSV / SQLite / local files", $tags="implemented_rel")
Rel(biencoder_api, vector_db_future, "Потенциальная замена in-memory индекса при масштабировании", $tags="assumption_rel")
Rel(model_runtime, fair_ltr_future, "Возможный второй этап переранжирования", $tags="assumption_rel")

SHOW_LEGEND()
@enduml