# Итоговый отчет: Сравнение стратегий ранжирования (Раздел 7.1)

#thesis #report #baseline #evaluation #bm25 #cross_encoder #bi_encoder #ranknet

## 1. Конфигурация бенчмарка

Сравнительный эксперимент охватывает три ранее зафиксированные модели и добавленный контур RankNet, реализующие принципиально разные стратегии ранжирования кандидатов:

1. **BM25, лексическая базовая линия (BM25 lexical ranking baseline)** — классическая модель информационного поиска без обучения. Токенизация выполняется простой регулярной токенизацией по словам с приведением к нижнему регистру. Оценка релевантности — BM25 между текстом вакансии (запрос) и текстом резюме (документ).
2. **Дообученный перекрёстный энкодер (fine-tuned cross-encoder)** — `cointegrated/rubert-tiny2` в архитектуре sequence classification для бинарной релевантности пары. Обучение на `train.tsv`, валидация на `val.tsv`, финальная оценка на `test.tsv`. Вход: `[vacancy_text, resume_text]`; выход: вероятность класса `label = 1`.
3. **Дообученный двухбашенный энкодер (fine-tuned bi-encoder)** — основная модель проекта `experiments/models/bi_encoder_rubert_tiny2`. Вакансия и резюме кодируются независимо; оценка — косинусное сходство между эмбеддингами (embeddings). Архитектура рассматривается как предлагаемый подход (proposed approach) для массового ранжирования в системе управления подбором персонала (Applicant Tracking System).
4. **RankNet, попарная нейросетевая базовая линия (pairwise Learning-to-Rank baseline)** — новый экспериментальный контур `experiments/scripts/19_train_ranknet.py`. Модель обучает скорер пары «вакансия–резюме» на предпочтениях `positive > negative` внутри одной вакансии и использует RankNet loss с логистической функцией от разности оценок. В текущей реализации это легковесный MLP на NumPy поверх стабильных hash-признаков текстовой пары, поэтому эксперимент не требует скачивания трансформеров и проверяет именно добавочную ценность pairwise Learning-to-Rank.

Скрипт `experiments/scripts/12_baseline_comparison.py` выполняет демонстрационный edge-case и полную оценку на `test.tsv` (`--evaluate_split`). Обучение перекрёстного энкодера — `experiments/scripts/18_train_cross_encoder.py`; обучение RankNet — `experiments/scripts/19_train_ranknet.py`.

## 2. Исследовательский смысл сравнения

### Подход 1: BM25
BM25 оценивает предел качества при совпадении терминов, навыков и должностей без обучения. Модель не требует нейросетевого инференса (inference) и интерпретируема, однако чувствительна к синонимии и перефразированию.

### Подход 2: перекрёстный энкодер
Перекрёстный энкодер оценивает верхнюю границу качества при попарном оценивании (pairwise scoring): каждая пара «вакансия–резюме» обрабатывается совместно через attention. Это повышает выразительность, но увеличивает стоимость инференса при больших пулах кандидатов.

### Подход 3: двухбашенный энкодер
Двухбашенный энкодер реализует масштабируемое семантическое ранжирование: эмбеддинги резюме можно предрассчитать и хранить в индексе, а при появлении вакансии вычислять только эмбеддинг запроса и косинусное сходство. Это снижает стоимость инференса и делает подход пригодным для интеграции в систему управления подбором персонала (Applicant Tracking System).

### Подход 4: RankNet
RankNet проверяет гипотезу о том, что имеющаяся попарная разметка может быть использована не только для бинарной классификации пар, но и для прямого обучения функции порядка. В отличие от BM25 модель обучаема, а в отличие от перекрёстного энкодера текущая реализация не выполняет совместный трансформерный inference для каждой пары. Поэтому RankNet добавлен как отдельный offline baseline: он полезен для методологического сравнения Learning-to-Rank, но не заменяет производственный bi-encoder-контур без отдельной интеграции в `ATSRanker`.

## 3. Результаты на `test.tsv` (521 пара, группировка по `vacancy_id`)

Оценка выполнена скриптом `12_baseline_comparison.py --evaluate_split --split test`. Агрегированные метрики:

| Модель | Средняя точность (Average Precision) | Нормализованный дисконтированный совокупный выигрыш на 3 (Normalized Discounted Cumulative Gain at 3) | Средний обратный ранг на 3 (Mean Reciprocal Rank at 3) |
|---|---:|---:|---:|
| BM25 | 0.3936 | 0.9888 | 0.9958 |
| Перекрёстный энкодер | 0.9356 | 1.0000 | 1.0000 |
| Двухбашенный энкодер | 0.9921 | 1.0000 | 1.0000 |
| RankNet | 0.3241 | 0.9888 | 0.9958 |

На уровне отдельных пар двухбашенный энкодер превосходит BM25 по средней точности (Average Precision) в **2.5 раза** (0.9921 против 0.3936) и перекрёстный энкодер — на **6.0%** (0.9921 против 0.9356). По нормализованному дисконтированному совокупному выигрышу на 3 (Normalized Discounted Cumulative Gain at 3) семантические модели достигают 1.0000 против 0.9888 у BM25. Полный прогон RankNet выполнен 2026-06-16 на приватной копии `data/` (Windows 10, Python 3.14, NumPy 2.4, CPU): `pair_average_precision = 0.3241`, метрики ранжирования внутри вакансии совпадают с BM25, что объясняется природой hashed-токенных признаков пары «вакансия–резюме». Артефакты: `experiments/models/ranknet_hashed_baseline/ranknet.npz`, `experiments/results/ranknet_test_metrics.json`, `experiments/results/four_model_baseline_test.json`, статус прогона — `experiments/results/ranknet_experiment_status.json`.

Ключевое методологическое наблюдение по сводным метрикам: pairwise-цель RankNet оказывается ограничена самой выборкой. В `train.tsv` из `2343` уникальных вакансий только `4` содержат одновременно положительный и отрицательный размеченный отклик, поэтому модель видит всего `48` предпочтительных пар (`positive > negative`). Дообученный двухбашенный энкодер, напротив, обучается на `4176` бинарных текстовых парах с `ContrastiveLoss`, поэтому имеет принципиально больше обучающего сигнала. Это объясняет, почему RankNet даже после 20 эпох сходится к лексическому уровню BM25, а bi-encoder остаётся лучшим по средней точности (`0.9921`). Сам факт расхождения подтверждает, что для практической интеграции pairwise L2R необходим либо отдельный сбор групп кандидатов с градуированной разметкой, либо гибридные признаки (оценка BM25, косинусное сходство bi-encoder, структурные сигналы) вместо чистого hashed-текста.

## 4. Демонстрационный edge-case (синонимия «Python-разработчик» / «программист-питонист»)

| Модель | Оценка релевантного кандидата | Позиция в ранжировании |
|---|---:|---:|
| BM25 | 1.8069 | 2 (выше оказался нерелевантный кандидат с лексическим совпадением «требуется») |
| Перекрёстный энкодер | 0.2971 | 4 |
| Двухбашенный энкодер | 0.9219 | 1 |

BM25 ошибочно поставил на первое место резюме с формальным совпадением слов «требуется», «разработчик», «опыт работы». Двухбашенный энкодер корректно выделил профиль Python-разработчика. На синтетическом edge-case перекрёстный энкодер после одной эпохи обучения не обобщился; основные выводы опираются на метрики `test.tsv`.

## Практические заключения для диссертации

По данным `test.tsv` двухбашенный энкодер стабильно превосходит лексическую базовую линию (средняя точность 0.9921 против 0.3936) и перекрёстный энкодер (0.9921 против 0.9356) при сопоставимых значениях нормализованного дисконтированного совокупного выигрыша на 3 и среднего обратного ранга на 3. Выбор между перекрёстным и двухбашенным энкодером определяется компромиссом между приростом средней точности (+6.0%) и стоимостью попарного инференса (inference) в контуре системы управления подбором персонала (Applicant Tracking System).

Добавление RankNet устраняет методологический пробел между теоретическим обзором Learning-to-Rank и экспериментальным протоколом: метод обучен и оценен на тех же `train.tsv`/`val.tsv`/`test.tsv` одной командой, и полученные численные значения зафиксированы в `experiments/results/ranknet_experiment_status.json` и `experiments/results/four_model_baseline_test.json`. По результатам полного прогона вывод о выборе основной архитектуры остается прежним: в продуктовый контур интегрирован bi-encoder, поскольку он поддерживает предрасчет эмбеддингов и быстрый поиск, а также по средней точности (`0.9921`) значительно превосходит как BM25 (`0.3936`), так и RankNet (`0.3241`) на текущей разметке. RankNet остается offline-baseline и заделом для будущего онлайн-переранжирования при условии накопления списков кандидатов с градуированной релевантностью.
